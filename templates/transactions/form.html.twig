{% extends "base.html.twig" %}

{% block content %}
    <div class="container mt-5">
        <div class="row">
            <div class="col-lg-6">
                <a href="{{ path("account_detail", {"id": app.request.attributes.get("id")}) }}">Back</a>

                <h1>Upload transactions</h1>

                {{ form_start(form) }}

                {{ form_row(form.file) }}

                <button type="submit" class="btn btn-lg btn-primary">Submit</button>

                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}